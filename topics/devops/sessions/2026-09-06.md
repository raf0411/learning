# Session — 2026-09-06

## Focus

Retrieve the missing-home warning versus `nologin` refusal and begin the formal
program/process/service/`systemd` probe.

## Concepts covered

- A program can exist as executable instructions on disk without a process.
- A process is a running instance of a program.
- A `.service` unit file is configuration; `ExecStart` names the program that
  the service manager should launch.
- Static configuration cannot itself remain running or take action.

## Successfully understood

- Correctly explained why `/usr/bin/python3` can exist as a program when no
  Python process exists.
- Correctly distinguished a `.service` unit file from the executable program
  named by `ExecStart`.

## Mistakes and uncertainties

- In the counterfactual with a real home and an unchanged `nologin` shell,
  expected both a missing-home warning and shell refusal. The full distinction
  was explained but not yet re-tested.
- Described `systemctl` as directly starting the service and `systemd` as an
  event recorder/logger.
- Correctly guessed that directly starting a program creates a process that is
  not thereby managed by a systemd unit, but reported low confidence.
- After identifying a `.service` file as configuration, selected it as the
  persistent component that can act after boot. The contradiction was surfaced,
  but the learner ended the session before answering the repair question.

## Exercises completed

- One account-home-versus-shell counterfactual.
- Written program-versus-process classification.
- Early control-chain and unit-versus-program probe questions.

## Commands and examples used

Conceptual examples used `/usr/bin/python3`, `systemctl start ssh.service`,
`sshd`, `systemd`, `.service` unit files, and `ExecStart`. No terminal commands
were executed.

## Concepts requiring review

- A real home removes the missing-home warning; `/usr/sbin/nologin` still
  refuses the login shell.
- `systemctl` is a client used to request management actions; it is not the
  persistent service manager.
- A `.service` file is passive configuration and cannot run or act.
- Separate `systemd`, `systemctl`, unit configuration, program, process,
  daemon, and service.

## Recommended next step

Start with a brief home-field-versus-shell-field retrieval. Then resume the
unfinished question: identify the running component that can read static unit
files and act after boot. Continue probing until the program/process,
daemon/service, and systemd-control strands each have a demonstrated floor and
ceiling; only then plan the lesson.
