# Session — 2026-09-15

Status: ended at the learner's request after shared-memory work, guided
mapping/residency repair, system-memory and swap inspection, and assignment
of a vmstat observation. No vmstat output was supplied before session end.

## Focus

Continue Phase 2 process and whole-system memory interpretation. Started with
the shared-block process-exit question left open on 2026-09-14, then inspected
a real Ubuntu shell process, system RAM, and the active swap file. Teaching
used small ASCII diagrams, a book analogy, and a named-physical-block table.

## Retrieval, mistakes, and repair

- Opening model: A and B each have a private 2 MiB block and both use the same
  resident 4 MiB block. A exits while B continues.
- The learner correctly identified A's private block as releasable and B's as
  remaining, but also expected part of the shared block to be released. The
  tutor clarified that both use the same entire block, rather than separate
  portions, and used two readers of one book as an analogy.
- The learner then guessed that none of the shared block could be released,
  explaining that B still needs it. This was a correct guided explanation.
- Asked to distinguish the combined physical memory from two RSS reports of
  6 MiB each, the learner said they did not understand the question. The tutor
  explained counting each actual physical block once: 2 + 4 + 2 = 8 MiB,
  whereas summing RSS counts the shared block twice and gives 12 MiB.
- Fresh smaller example: 1 MiB private per process and the same shared 3 MiB
  block. The learner answered 3 + 3 = 6 MiB, counting shared users as copies
  and omitting the private blocks. Thus the explanation alone had not
  established the accounting model.
- A table named the physical blocks X (1 MiB, A), Y (3 MiB, A and B), and
  Z (1 MiB, B). The tutor supplied the physical total 1 + 3 + 1 = 5 MiB.
- Asked whether C also using the exact same Y enlarges it or adds another
  user, the learner correctly explained that there is simply another user;
  the block does not get bigger. The learner attributed the earlier answer
  to not thinking straight. Record this as self-report, not evidence that the
  earlier error was only a typo or that independent accounting is established.

## Live read-only observation

The learner ran the supplied command successfully at the Ubuntu prompt:

```bash
top -b -n 1 -e k -p "$$"
```

`$$` selected the current shell's PID, and `-e k` selected KiB for task-memory
columns. The supplied output's system-memory summary separately used MiB.

Relevant actual output, including the right-edge truncation as supplied:

```text
top - 20:36:33 up  9:19,  1 user,  load average: 0.41, 0.47, 0.42
Tasks:   1 total,   0 running,   1 sleeping,   0 stopped,   0 zombie

    PID USER      PR  NI    VIRT    RES    SHR S  %CPU  %MEM     TIME+
 460670 raf_0411  20   0    8784   5716   3940 S   0.0   0.1   0:00.03
```

This was observation only. No new disposable process requiring cleanup was
created, and no process was signaled. Do not reuse this PID for later actions
without verifying current identity.

## Interpretation and mastery evidence

- Before the command, the tutor explained RES as resident process memory and
  SHR as a potentially shareable portion already within RES. The caveat that
  SHR does not prove another current user was also briefly stated.
- The tutor proposed the incorrect sum 5716 + 3940 = 9656 KiB, asking whether
  the learner agreed and prompting them to consider where SHR fits in RES.
- The learner correctly rejected the addition: "its part of RES which is
  resident, which doesnt make sense to add them all together".
- Shared physical blocks — GUIDED. The user-versus-copy distinction was
  explained correctly with scaffolding. Independent calculation of combined
  physical memory remains unproven.
- SHR within RES — GUIDED, with correct interpretation of actual output after
  teaching and a subset prompt. No claim of INDEPENDENT, RETAINED, or
  TRANSFERABLE mastery follows from this check.
- Command execution — the supplied command was submitted cleanly on one line.
- CPU normalization, exact active sharing, and how much system RAM a process
  exit would make available were not assessed today.

## Mapping/residency retrieval and repair

- Using the actual row's VIRT 8784 and RES 5716, the tutor asked the learner
  to explain the distinction without immediate reteaching.
- The learner described VIRT as virtual memory not reflecting physical RAM
  "yet" and RES as resident or waiting/free memory, explicitly reporting a
  vague idea and guessing. These are conceptual errors, not a retained answer.
- The tutor explained that resident means currently in RAM and that VIRT
  includes mappings whose pages are already resident as well as nonresident
  mappings.
- A four-page example showed mappings A B C D and residency A C. Asked to
  update both lines when B entered RAM without changing mappings, the learner
  correctly wrote mappings A B C D and residency A B C.
- Mapping/residency remains GUIDED. The tutor supplied the explanation and
  representation immediately before that correct reconstruction. No live page
  residency was changed during this step.

## Whole-system RAM and cache

The learner ran the supplied read-only command and pasted:

```text
raf_0411@homeless-lab:~$ free -h
               total        used        free      shared  buff/cache   available
Mem:           7.2Gi       1.2Gi       3.9Gi        19Mi       2.4Gi       6.0Gi
Swap:          4.0Gi          0B       4.0Gi
raf_0411@homeless-lab:~$
```

- The tutor explained free as unused RAM, buffers/cache as occupied memory
  with a reclaimable portion, and available as the estimated capacity for
  applications without swapping. Not all buffers/cache were claimed to be
  immediately reclaimable; available is not an additional disjoint allocation.
- File caching was introduced as keeping copies in RAM for faster repeated
  reads. The example explicitly concerned a cached copy matching a saved file
  on disk, which can be discarded while the original remains.
- Asked where discarded file data could be obtained again, the learner
  proposed the 6.0 GiB available figure, reasoning that it includes reclaimable
  memory. The tutor separated the data source from the amount of RAM space.
- A concrete saved file containing hello showed the original remaining on disk
  after its RAM copy is discarded; a later read can copy the data back to RAM.
- The learner then described disk data as kept, RAM as short-lived, and data
  as recoverable from disk. The core saved-original/temporary-copy distinction
  was retrieved with guidance. Their phrase "reclaim memories from disk"
  prompted clarification: reclaim RAM space versus reload file data. The tutor
  also introduced volatile RAM and emphasized the need for an up-to-date disk
  copy before a cached copy can simply be discarded.
- In a hypothetical free 200 MiB / available 5 GiB scenario, the learner
  correctly rejected low free alone as proof of a shortage, but reasoned that
  Linux could "add more from the available 5 GiB". The tutor clarified that
  available estimates capacity within existing RAM and includes the already
  free portion; reclaiming cache reuses RAM instead of adding physical RAM.
  The learner acknowledged this explanation without another independent attempt.
- System-memory interpretation remains GUIDED. A correct conclusion with
  imprecise pool/addition reasoning does not establish independent operational
  interpretation. No cache was manually dropped, memory pressure induced,
  swap changed, or file modified.

## Swap inspection and residency direction

The tutor motivated swap using application data that has no existing saved
file copy. The learner ran the supplied read-only command and pasted:

```text
raf_0411@homeless-lab:~$ swapon --show
NAME      TYPE SIZE USED PRIO
/swap.img file   4G   0B   -2
raf_0411@homeless-lab:~$
```

- The tutor interpreted this as a 4 GiB active swap file with no occupied
  swap capacity in that snapshot. PRIO was not taught or assessed.
- A simplified sequence explained preserving a page's data in the swap file,
  releasing its physical RAM page, and loading the data back into RAM when
  needed. The process's virtual mapping can remain throughout.
- Asked what happens to VIRT and RES when a page leaves RAM for swap while
  its mapping remains, the learner correctly kept VIRT unchanged but predicted
  an increase in RES and said the page becomes resident.
- The tutor explicitly separated the two directions: RAM to swap decreases
  RES once the page is no longer resident; swap to RAM increases RES. VIRT is
  unchanged in this example. A mapped A B C D / resident A B C model was
  changed to mapped A B C D / resident A C when B left RAM.
- The tutor noted that the learner's answer would match the return trip in
  step 3. It is not established whether the learner followed the wrong stage
  or reversed the residency concept. The learner acknowledged the correction,
  but did not attempt a fresh prediction before session end.
- Swap-area inspection is GUIDED command execution. Swap-out versus swap-in
  residency direction remains unresolved; acknowledgement is not evidence of
  successful retrieval.

## Pending practical work

The tutor introduced swap occupancy versus transfer activity and assigned:

```bash
vmstat -y 1 3
```

The explanation covered one-second reporting, skipping the initial since-boot
report with -y, si as swap to RAM per second, and so as RAM to swap per second.
The learner ended the session without providing output. Execution and field
interpretation are therefore unverified. Do not invent rows or conclude that
si/so were zero from the earlier unused-swap snapshots.

All completed terminal work today was observation. No new disposable lab
resources need cleanup, and no swap or memory-pressure experiment was run.

## End-of-session learner model

- Demonstrated with guidance: shared users do not create extra copies;
  SHR is included in RES in an actual row; a resident page remains mapped;
  a saved file can supply data again after its clean RAM copy is discarded.
- Successfully executed supplied top, free, and swapon observation commands.
- Review priorities: counting distinct physical blocks; VIRT/RES and swap
  direction; available as an estimate within existing RAM rather than a source
  of extra RAM or file contents; reclaiming space versus reloading data.
- Memory concepts remain GUIDED where successful scaffolded evidence exists.
  Independent shared-block totals and swap-direction repair are unproven.
  No new INDEPENDENT, RETAINED, or TRANSFERABLE mastery is recorded today.

## Next step

Begin with one short, clearly staged swap-direction retrieval. Specify whether
the page has left RAM or returned to RAM, and ask for VIRT/RES changes without
giving the direction rule first. Repair only as much as needed, then run the
pending read-only vmstat observation on Ubuntu and interpret actual si/so
values. Keep the observation window explicit and avoid inducing memory pressure
solely to obtain nonzero output. Revisit free/available reasoning and physical
block totals after spacing, without repeating the entire lesson.

## Reference and worksheet

- [Process and system memory worksheet](../diagrams/2026-09-15-shared-memory.md)
- [top manual](https://man7.org/linux/man-pages/man1/top.1.html) — checked the
  RES and SHR definitions, batch/iteration options, task-memory units, and PID
  filtering. No need to duplicate the existing resource entry in RESOURCES.md.
- [free manual](https://man7.org/linux/man-pages/man1/free.1.html) — checked
  free, buffers/cache, available, and human-readable units; added to resources
  for continued whole-system memory work.
- [Linux memory concepts](https://docs.kernel.org/admin-guide/mm/concepts.html)
  — page cache, anonymous memory, and reclaim.
- [swapon manual](https://man7.org/linux/man-pages/man8/swapon.8.html) — verified
  that --show lists active swap areas.
- [vmstat manual](https://man7.org/linux/man-pages/man8/vmstat.8.html) — verified
  delay/count, -y, and si/so definitions for the pending observation.
