# Session — 2026-09-11

## Focus

Retarget the learning path toward entry-level DevOps and Cloud roles, then
continue the `ps`/`top` CPU interpretation lesson on the physical Ubuntu home
server.

## Curriculum decision

- Changed the primary career target from SysAdmin/Linux administration to
  entry-level DevOps Engineer, Junior Cloud Infrastructure Engineer, InfraOps
  Engineer, Cloud Operations/Support Engineer, or a closely related role.
- Kept Linux/SysAdmin roles as useful secondary entry routes.
- Preserved all completed Linux work because process, service, networking,
  security, observation, and troubleshooting skills remain prerequisites for
  the new target.
- Selected AWS as the primary cloud and made Git, Bash, Docker, CI/CD,
  Terraform, and observability required outcomes.
- Moved Git and automation earlier, connected later tools through one delivery
  project, and deferred deep Kubernetes and multi-cloud operation until after
  the entry-level exit gate.
- Updated `GOAL.md` and materially reorganized future phases in `ROADMAP.md`.
  The current Phase 2 position did not change.

## Concepts covered

- A `top` frame is the state sampled at one refresh instant, not the whole time
  interval until the next refresh.
- An already-running process can appear in the first frame. A process whose
  entire lifetime falls between two refreshes can be missed by both frames.
- Installed `top(1)` documentation defines task `%CPU` over the interval since
  the last screen update; procps-ng `ps` reports a lifetime average.
- CPU time divided by elapsed wall time gives a unitless utilization ratio;
  multiplying by 100 expresses it as a percentage.
- A socket is a physical CPU package, a core is a physical execution core, and
  a logical CPU is a hardware thread exposed to the operating system.
- `nproc` reports logical processing units available to the current process; it
  does not by itself reveal the number of physical cores.
- In `top` Irix mode, one fully busy logical CPU corresponds to about `100%`, so
  a multi-threaded process may exceed `100%`. In Solaris mode, that value is
  divided by the total visible logical CPUs so whole-machine capacity is `100%`.
- A PID-filtered `top` view reports zero tasks once the selected PID has exited.

## Successfully understood

- After clarifying the word “frame,” correctly placed an already-running
  `backup-job` in the `t=0` frame and a later continuing `api-worker` in the
  `t=2` and `t=4` frames.
- Correctly calculated a fresh example as `ps = 20/200 = 10%` and
  `top = 2/4 = 50%`, separating lifetime and recent measurement windows.
- Used the installed `top(1)` page to locate the actual `%CPU` definition after
  the longer suggested search phrase was absent.
- Ran `nproc` and observed 16 available logical CPUs.
- Ran `lscpu` and observed one socket, eight cores per socket, and two threads
  per core, accounting for 16 logical CPUs.
- Correctly calculated a Solaris-mode example as `3/8 = 37.5%` and later
  identified one fully busy logical CPU as `100%` in Irix mode.
- Created and identity-checked bounded disposable `yes` PID `57467`. The first
  `ps` showed it as the expected user-owned `yes` process in state `R`, with
  lifetime `%CPU` near `100`.
- Correctly diagnosed the subsequent evidence: the CPU-time limit killed the
  process, the PID-filtered `top` view therefore contained zero tasks, an empty
  `ps` proved absence, and no further termination signal was necessary.

## Mistakes and corrections

- Initially treated the interval from `t=0` to `t=2` as a continuously observed
  `top` frame, so predicted that a process existing only between the refreshes
  would appear. Corrected after “frame” was defined as one sampled instant.
- Initially guessed that `ps` could not show a low value while `top` showed a
  high value for the same process. A numerical example established that their
  lifetime and recent windows can legitimately produce both values.
- Repeatedly changed the Irix scale: at different points treated one fully busy
  logical CPU as `200%`, three as `600%`, or four of sixteen as only `100%`.
  Corrected rule: one fully busy logical CPU is about `100%` in Irix mode.
- For the actual 16-logical-CPU server, first used `1/1` as the Solaris denominator
  and converted it to `0%`. Correct denominator is all 16 logical CPUs, so one
  fully busy logical CPU is `1/16 = 6.25%` in Solaris mode.
- The 30-CPU-second disposable process expired before both modes could be
  observed. This produced a useful sampling and cleanup diagnosis but did not
  complete the Irix/Solaris comparison.
- Ran `kill -TERM` after the identity check had already returned no process row,
  receiving `No such process`. The learner then correctly explained that the
  action was unnecessary; preserve the conditional identity-check habit.

## Exercises and commands

- Discrete-refresh predictions using processes that existed before, between,
  and after sample instants.
- Lifetime-versus-recent `%CPU` calculations.
- Installed `man top` lookup of `%CPU -- CPU Usage`.
- CPU-topology inspection:
  - `nproc`
  - `lscpu | grep -E '^(CPU\(s\)|Thread\(s\) per core|Core\(s\) per socket|Socket\(s\)):'`
- Bounded CPU-process experiment:
  - `bash -c 'ulimit -t 30; exec yes' > /dev/null & cpu_lab_pid=$!`
  - identity and lifecycle inspection with `ps -p "$cpu_lab_pid" ...`
  - `top -p "$cpu_lab_pid"`
  - final `kill`, `sleep`, and empty `ps` verification.

## Mastery evidence

- Discrete `top` refresh model — INDEPENDENT in a fresh written scenario after
  one terminology repair.
- `ps` lifetime versus `top` recent interval — GUIDED. One fresh numerical
  calculation was correct, but the initial operational scenario required a
  worked example.
- Physical cores versus logical CPUs — GUIDED. Actual topology was inspected
  and reconstructed with the tutor; independent transfer remains untested.
- Irix versus Solaris normalization — GUIDED and unstable. Some normalized
  calculations were correct, but the per-logical-CPU scale and denominator were
  repeatedly confused.
- Conditional cleanup after PID inspection — previously demonstrated
  independently, but today an unnecessary `kill` followed an empty check. Keep
  it in operational review rather than downgrading the entire termination skill.

## Concepts requiring review

- One fully busy logical CPU is approximately `100%` in Irix mode; `N` fully
  busy logical CPUs can contribute approximately `N × 100%` to one process.
- Solaris mode divides the Irix-style value by all logical CPUs visible to
  `top`; on this 16-logical-CPU server, one fully busy logical CPU is `6.25%`.
- Act on a PID only when the fresh identity check still shows the intended
  process.
- Continue later spaced retrieval for `fg` versus `bg`, signal disposition, and
  the safe TERM/wait/inspect/conditional-KILL sequence.

## Environment evidence

- Current Ubuntu work is being performed on a physical home server, not a VM.
- CPU topology reported by `lscpu`: one socket, eight physical cores, two
  threads per core, and sixteen logical CPUs.
- Disposable PID `57467` was absent in the final `ps` snapshots. No process from
  today's CPU lab remains.

## Recommended next step

Repeat the bounded `yes` observation with the assigned 60-CPU-second limit and
`top -d 1`, record one value with Irix mode on and one with it off, restore the
original mode, then conditionally clean up only if a fresh identity check still
shows the same process. After independently explaining the two values, continue
to `VIRT`, `RES`/RSS, and `SHR`.

## Verification

- Curriculum direction was checked against current entry-level Cloud
  Infrastructure, InfraOps, and DevOps role requirements before the roadmap was
  revised.
- All CPU behavior recorded above came from the learner's installed `top(1)`
  manual and actual output from the Ubuntu home server.
