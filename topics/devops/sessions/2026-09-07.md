# Session — 2026-09-07

## Focus

Complete the transient-service lifecycle check, probe signals/job control/
resource inspection, and begin the planned signals lesson. A short same-day
continuation focused on repairing signal delivery versus signal outcome.

## Concepts covered

- `active (exited)` describes a service unit with no continuing main process;
  it does not mean an exited daemon is still running.
- A finite `sleep` exits naturally when its duration ends. Its `Type=exec`
  transient service then becomes inactive, and `--collect` allows systemd to
  unload the unit.
- Signal delivery and signal outcome are separate. A signal disposition may
  specify a default action, a registered handler, or ignoring the signal. This
  foundation was introduced but did not yet land.
- A signal handler is code executed in the process. If the handler returns,
  normal execution resumes; a handler does not inherently stop or terminate
  the process.
- Terminated means the process has ended. Stopped means it still exists but its
  execution is suspended and may later be resumed.
- The next lesson will separate running/stopped/terminated state from
  foreground/background placement, then cover safe termination and process
  inspection.

## Successfully understood

- Correctly mapped the passive unit file, managed service unit, daemon process,
  persistent `systemd` manager, and temporary `systemctl` client.
- After one correction, identified the service unit—not a daemon—as the object
  that can remain `active (exited)` with no process.
- Verified from live Ubuntu output that the old transient unit was unloaded and
  PID `236595` was absent.
- After correction, transferred the chain `sleep` exits naturally → unit
  becomes inactive → `--collect` unloads it to a fresh 10-second example.
- Correctly recognized that `Ctrl+Z` pauses a process and that `ps` is a
  short-lived snapshot while `top` is a refreshing display.

## Mistakes and uncertainties

- Initially described `active (exited)` as a daemon that was no longer running
  and suggested that the state might itself indicate a crash.
- Predicted that both the transient unit and its former PID would still exist.
- Initially attributed the observed cleanup to the service killing itself and
  to a manual stop; neither happened.
- Treated `Ctrl+C` and plain `kill PID` as unavoidable forced destruction.
- Does not yet know `&`, `jobs`, `fg`, or `bg`, or how to resume a stopped job.
- Interpreted one process near 100% CPU as necessarily exhausting a four-core
  machine.
- After being taught that signal delivery and outcome are separate, selected
  immediate kernel destruction even though the scenario specified a registered
  cleanup handler. The signal foundation needs repair before progression.
- In the continuation, mapped a terminating default action to "stopping," a
  cleanup-and-exit handler to pausing, and an ignored signal to exiting.
- After correction, interpreted a handler that increments a counter and returns
  as appending stopped processes that could later resume. This conflated data
  changed by handler code with process state. The simpler handler-return check
  was posed but not answered before session end.

## Exercises completed

- Service-role retrieval and `active (exited)` correction check.
- Read-only transient-unit and former-PID verification.
- Fresh-example transfer check for natural exit, inactivity, and collection.
- Mapping probe for signals, Bash job control, and resource inspection.
- Reviewed and approved the dependency plan for the next lesson.
- Same-signal/default-handler-ignore retrieval attempt and focused repair.

## Commands and examples used

- `systemctl show tutor-lifecycle-20260906 -p LoadState -p ActiveState -p SubState -p MainPID`
- `ps -p 236595 -o pid,ppid,user,comm,args`
- Hypothetical `sleep 300`, `sleep 300 &`, and `kill 4242` examples.
- Instructor verification used the local `sigaction(2)` manual and the POSIX
  signal-action specification before correcting handler behavior.

Observed service properties were `MainPID=0`, `LoadState=not-found`,
`ActiveState=inactive`, and `SubState=dead`. The `ps` command returned only its
header, confirming that PID `236595` did not exist.

## Concepts requiring review

- Spaced retrieval of service unit versus daemon and PID versus PPID.
- Signal delivery versus signal outcome; a registered handler must be allowed
  to determine the process response for a catchable signal.
- Default `SIGTERM` versus forced `SIGKILL` has not yet been taught.
- Stopped versus background state, Bash job control, and multicore CPU
  percentages remain new.

## Recommended next step

Resume with the focused number-printing example: a handler prints
`SIGNAL NOTED` and returns, after which the learner must predict process state
and control flow. Then re-test default action, handler, and ignore together. Do
not build further until signal delivery and outcome are separated; afterward,
proceed to the disposable `sleep` job-control lab.
