# Session — 2026-09-10

## Focus

Complete and verify safe conditional process termination, then probe and plan
the `ps`/`top` CPU and memory interpretation lesson.

## Concepts covered

- A successful `kill` reports signal-request acceptance, while a later `ps`
  snapshot provides separate evidence about whether a PID is present.
- A returning `SIGTERM` handler lets interrupted execution continue.
- `SIGKILL` cannot be caught or ignored and gives the process no application
  cleanup opportunity.
- Safe escalation is conditional: verify identity, send `SIGTERM`, wait,
  inspect, send `SIGKILL` only if still present, wait, then inspect again.
- `ps` is a short-lived snapshot command; `top` stays in the foreground and
  displays discrete, repeated refreshes rather than continuous omniscience.
- On a four-worker capacity model, one fully busy worker uses one quarter of
  total capacity.
- A process need not load an entire opened file into physical RAM; virtual
  address space versus resident and shared pages remains the next lesson.

## Successfully understood

- Correctly explained that a returning handler leaves the process running.
- Correctly separated signal-request success from evidence of process presence.
- Predicted that the disposable handler process would survive `SIGTERM` but be
  absent after `SIGKILL`.
- Verified old PID `375142` was absent before attempting any cleanup.
- Created and identity-checked disposable handler PID `5767` on Ubuntu.
- Sent `SIGTERM`, waited, and used a fresh `ps` snapshot to prove PID `5767`
  remained after its handler returned.
- Correctly made escalation depend on that observation, sent `SIGKILL`, waited,
  and verified PID `5767` was absent.
- Described the broad `ps` snapshot versus `top` refresh distinction.
- Derived one busy worker out of four as one quarter of machine capacity.
- Correctly expected partial file data, rather than an entire file, to occupy
  RAM when only part is needed.

## Mistakes and uncertainties

- Initially repeated `SIGTERM` where the second-stage signal had to be
  `SIGKILL`.
- In a written rehearsal, used `resistant_pid` in both inspection commands
  instead of the assigned `resistant_lab_pid`; corrected before the live flow.
- Initially listed all six operations together, which would make `SIGKILL`
  unconditional if pasted rather than waiting for the first inspection.
- One paste included bracketed-paste control characters and produced
  `sleep: command not found`; the intended `sleep 2` was immediately rerun.
  Treat this as an input artifact, not a process-control misconception.
- Does not yet know how procps-ng process `%CPU` relates to multicore capacity.
- Does not yet know `VIRT`, `RES`/RSS, or `SHR` memory semantics.

## Exercises completed

- Opening retrieval on returning handlers, `kill` versus `ps`, and exact safe
  escalation commands.
- Read-only identity check of the last-observed PID from the prior session.
- Live returning-handler process lab on the Ubuntu VirtualBox VM.
- Full `SIGTERM`-wait-inspect-conditional-`SIGKILL`-wait-inspect sequence.
- Initial and narrowed process-resource probe.
- Reviewed and approved the dependency plan for CPU and memory interpretation.

## Commands and examples used

- `ps -p 375142 -o user=,pid=,ppid=,stat=,lstart=,command=`
- `bash -c 'trap "echo SIGTERM handled: continuing" TERM; while true; do sleep 1; done' &`
- `resistant_lab_pid=$!`
- `ps -p "$resistant_lab_pid" -o user=,pid=,ppid=,stat=,command=`
- `kill -TERM "$resistant_lab_pid"`
- `sleep 2`
- `ps -p "$resistant_lab_pid" -o pid=,stat=,command=`
- `kill -KILL "$resistant_lab_pid"`

Ubuntu output showed handler PID `5767` survive `SIGTERM` and disappear after
`SIGKILL`. The final `ps` printed no process row.

## Concepts requiring review

- Preserve the same verified PID variable across action and inspection.
- Pause for and interpret the first inspection before deciding on `SIGKILL`.
- Distinguish a handler returning from a signal “returning.”
- Keep `fg` versus `bg` and signal disposition in spaced review.
- Complete snapshot/refresh timing, multicore CPU percentages, and
  virtual-versus-resident memory.

## Recommended next step

Resume the pending sampling check: if `top` refreshes at `t=0` and `t=2`, can
it display a process that exists only from `t=0.5` to `t=1.5`? After confirming
that node, verify `ps --version`, `top --version`, and the installed Ubuntu
manuals before deriving their different `%CPU` measurement windows.

## Verification

- Live learner-machine output verified both the old PID's absence and the fresh
  disposable process's `SIGTERM`/`SIGKILL` outcomes.
- Local Bash `help kill`, previously recorded signal manuals, upstream
  procps-ng `ps(1)`/`top(1)` manuals, and Linux kernel `/proc` documentation
  informed the technical plan. Links are recorded in `RESOURCES.md`.
- The first snapshot-versus-refresh node was introduced, but its check was not
  attempted before session end.
