# Session — 2026-08-26

## Focus

Established the initial skills baseline and created a dependency-based,
job-focused roadmap for entry-level Linux/SysAdmin and Cloud Support roles.

## Concepts covered

- Absolute paths versus relative paths.
- Commands act on the path arguments they receive.
- Creating a directory does not change the shell's current directory.
- Whitespace separates a command name from its arguments; a slash is part of a
  path and does not serve as that separator.
- The initial sequence of priorities: Linux/filesystem, core Ubuntu
  administration, networking, operations, services, automation, then cloud.

## Successfully understood

- Correctly explained absolute and relative paths.
- Correctly predicted that `mkdir practice` followed by `touch notes.txt` leaves
  the shell in `/home/raffi` and creates `/home/raffi/practice` and
  `/home/raffi/notes.txt`.
- Correctly explained that the file was outside `practice` because the shell had
  not entered that directory, then identified `touch practice/notes.txt` as the
  command that targets it directly.
- Recalled basic navigation and inspection commands.
- Correctly formed an SSH command using a username and IP address.
- Recalled basic `mkdir`, `touch`, `cp`, and `rm` usage.
- Recalled Ubuntu package update/upgrade commands.
- Connected DNS with resolving a website name to an address.
- Recognized pipes as a way to pass output to another command such as `grep`.

## Mistakes and uncertainties

- Created `notes.txt` in the current directory rather than inside the newly
  created `practice` directory.
- Briefly considered `touch/practice/notes.txt`; needs to retain that the space
  separates the `touch` command from its path argument.
- Did not know the file rename/move operation.
- Could not interpret Unix permission notation reliably.
- Conflated services, processes, `systemd`, and the commands used to control
  services.
- Did not know the usual disk, OS, IP, or log inspection commands.
- Had incomplete models of localhost, private IP addresses, and gateways.
- Could not recall a concrete homelab configuration or troubleshooting example.

## Exercises completed

- Completed a broad and a focused skills probe using explanations, commands,
  and troubleshooting scenarios.
- Completed a short retrieval quiz on current-directory state and relative path
  resolution without using the terminal.
- Composed most of a basic file-management command sequence.
- The assigned Ubuntu file-management lab was not attempted before session end.

## Commands and examples used

`pwd`, `ls`, `ls -la`, `whoami`, `neofetch`, `top`, `htop`, `mkdir`, `touch`,
`cp`, `rm`, `ssh raffi@192.168.1.50`, `sudo apt update`, `sudo apt upgrade`,
`curl`, `grep`, and `nano`.

Additional command discussed: `touch practice/notes.txt`.

## Concepts requiring review

- Working-directory state and command targets.
- Move/rename operations and safe file handling.
- Permissions and ownership.
- Processes, services, `systemd`, and `systemctl`.
- Basic system inspection and networking concepts.
- Pipes and redirection.

## Recommended next step

At the Ubuntu terminal, complete the pending `sysadmin-lab-01` exercise: verify
the current directory, create the lab directory, create `notes.txt` inside it
without entering it, and inspect the result. Then continue with copy, rename,
and safe deletion before moving to text inspection and pipelines.
