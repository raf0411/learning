# 2026-09-24 — model invariants and unfinished Remove behavior

Status: session ended at the learner's request. Compilation and runtime evidence
is learner-reported Xcode Playground output supplied in chat; the tutor did not
directly observe the run.

## Protecting the ShoppingList invariant

- Predicted that direct external append to the exposed item array compiles, bypasses
  `ShoppingList.add`, and admits an empty name with quantity zero. Correctly gave
  the resulting count `2`, empty-name comparison `true`, and quantity `0`.
- Identified the bypass generally as avoiding the model Add rules; clarified that
  the intended invariants are nonblank normalized names, positive quantities, and
  no exact duplicates.
- Initially predicted that adding `private(set)` would reject the model's own Add
  call, allow earlier reads to execute, and prevent Add from appending. Corrected
  the mental model: setter access depends on where mutation code is implemented,
  and a compile error prevents the whole program from running.
- Added `private(set)` to the owned array. Reported the compiler diagnostic
  `Cannot use mutating member on immutable value: 'list' setter is inaccessible`
  for caller-side append. After commenting out only that append, reported output
  `3` and `Bread`, showing that caller reads and model-owned mutation still work.
- Explained in their own words that outside callers retain read access but cannot
  mutate the array directly.

## Remove requirement trace and enum design

- For three sequential Remove calls, correctly determined cleaned names, branch
  outcomes, and count transitions: removing Eggs changes 2 to 1; a second Eggs
  attempt and a blank name both leave the count at 1.
- Initially used array contents for the requested `result` field and changed the
  exact required success text from `1 items remain` to `1 item remain`. After the
  distinction was explained, independently wrote
  `RemoveResult.removed(name:remainingCount:)`, `.notFound(name:)`, and `.emptyName`.
- Wrote an exhaustive caller-side switch without enum syntax scaffolding and
  extracted both associated values correctly. The success message again used
  `item remain`, and the first test input was changed from the assigned `" Eggs "`
  to `" Eggs   "`.

## Debugging the incomplete Remove method

- The first implementation cleaned the input and rejected blank names, but assigned
  the result of `contains` to `isNotFound`. Its guard therefore returned `.notFound`
  when a matching Eggs item existed. The actual removal remained commented out,
  and the method could return success without changing the array.
- After a focused trace prompt, correctly stated that `contains` returns `true` for
  Eggs, the inverted guard returns `.notFound`, and no item is removed.
- Introduced the next repair concept: `contains` supplies only a Bool, whereas
  removal requires the matching position. `firstIndex(where:)` returns an optional
  index that can distinguish the not-found and removal paths.

## Current checkpoint and next step

- `ShoppingList` now protects its array setter with `private(set)`, and the learner
  has verified both the rejected external mutation path and the permitted validated
  Add path.
- Result-enum construction and exhaustive switching were independently reproduced
  for a new Remove domain after spacing.
- Remove is not implemented or tested successfully. Resume by writing the
  `firstIndex(where:)` predicate, safely unwrapping the index, calling
  `remove(at:)`, calculating count after mutation, restoring the exact three calls
  and exact message text, and reporting actual output plus final state.
