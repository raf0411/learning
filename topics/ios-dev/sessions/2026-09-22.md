# 2026-09-22 — reusable validation and first model methods

Status: two learning blocks ended at the learner's request. Execution evidence
is learner-reported Xcode Playground output supplied in chat; the tutor did not
directly observe the runs.

## Duplicate search and Add refactor

- Implemented `containsItem(named:items:) -> Bool` using a loop, exact name
  comparison, an early `true`, and a final `false` without being given its internal
  structure. The first test used conditional custom messages rather than printing
  both Boolean results and included output inconsistent with the submitted code.
  After clarification, the learner printed and reported `false` for missing Bread
  and `true` for existing Eggs.
- Replaced inline duplicate detection in the Add behavior with this function.
  Added the required Foundation import, trimmed `"   Bread  "`, appended only after
  validation, and displayed the list. The learner-reported valid output was Milk,
  Eggs, and Bread with unpurchased markers.
- Predicted and reported the duplicate and whitespace-only branches with the list
  unchanged. This completed the previously pending reusable search task.

## Optional conversion and reusable quantity validation

- Initially predicted that `Int("hello")` would crash and hold `"hello"`. A direct
  experiment reported `Optional<Int>`/`Optional(20)` for valid input and
  `Optional<Int>`/`nil` for invalid input.
- Used `if let` to distinguish a positive quantity, failed conversion, and zero.
  Predictions and reported output matched for `"4"`, `"four"`, and `"0"`.
- Correctly proposed `Int?` for a reusable `validQuantity` function and `nil` for
  invalid cases. The function itself correctly rejected failed conversion, zero,
  and negative values.
- The caller initially checked only `!= nil`, discarded the returned integer,
  changed the requested argument label, and treated angle-bracket placeholders as
  literal output. After progressively stronger prompts, it used `if let`, the
  requested `from:` label, and the unwrapped value. Input `"004"` then produced
  `004 -> accepted: 4`, and the learner explained that the left side was the
  source String while the right side was the converted Int.

## Quantity-aware shopping-item change

- The first attempt made stored quantity optional, declared the item array with
  `let`, called `append()` without the newly created item, omitted quantities from
  display, and skipped requested planning details.
- After targeted questions, the learner explained that stored quantity should be
  `Int`: invalid optional conversion prevents appending, so every stored item has a
  valid value. Changed the array to `var`, called `append(newItem)`, printed each
  quantity, and completed an ordered trace covering cleaning, empty/duplicate/
  quantity checks, conditional mutation, and final display.
- Learner-reported valid output matched exactly for Milk quantity 1, Eggs quantity
  12, and Bread quantity 2 converted from `"02"`.
- Tested Bread with quantity `"0"`; the quantity error appeared and the list stayed
  unchanged. Tested duplicate Eggs with invalid quantity; duplicate validation won
  because it occurred first. The predicted branch was right, but the predicted
  generic message did not match the code's interpolated `Eggs already exists!`.

## First `ShoppingList` methods

- Recognized that a `ShoppingList` struct would own an item array and Add method,
  but did not know whether a method could mutate it. After instruction on
  `mutating` and the need for a `var` instance, independently wrote the small
  struct, added Milk, and reported an item count of `1`.
- Implemented `containsItem(named:)` inside `ShoppingList`, correctly searching
  its own array without a separate array parameter or `mutating`. The first run
  predicted `true`/`false` but reported `false`/`false` because Milk had been
  constructed but never added. After identifying the omitted `list.add(milk)`
  call, the learner reported `true`/`false`.

## Mastery evidence and stopping point

- Exact-name array search with a Bool-returning function reached INDEPENDENT in
  this narrow current-session task; retention and transfer are not established.
- Optional conversion, safe extraction, stored model design, and struct mutation
  are GUIDED. The learner performed each successfully but received syntax,
  decomposition, or correction prompts.
- Requirement tracing remains GUIDED. Small-code logic was often sound while the
  written plan, test setup, message text, or requested output omitted a detail.
- Enums were unfamiliar. The tutor introduced `AddResult` with `added`,
  `emptyName`, `duplicateName`, and `invalidQuantity` cases and showed exhaustive
  `switch` structure. The assigned switch/message exercise was not attempted before
  session end, so enum performance remains unassessed.

## Later learning block: `AddResult` and exhaustive switching

- Independently wrote the four-case `AddResult` enum, iterated over an array of
  results, and used a `switch` that handled every case with the required message.
  Learner-reported output exactly matched the submitted code.
- Changed the supplied result order by swapping the first two cases. The initial
  prediction therefore matched the learner's code but not the assigned test
  scenario. After this was identified, accurately predicted the original order.
- Did not initially know why the switch required no `default`. After instruction
  that Swift can verify all enum cases, predicted that adding an unhandled fifth
  case would prevent compilation.
- The first compiler experiment mistakenly added an empty case to the switch and
  produced the error that a case needs an executable statement. After keeping the
  new case only in the enum, reported `Switch must be exhaustive` and correctly
  explained that `.listFull` was missing.
- Enum declaration and switching are GUIDED: the complete implementation was
  correct, but the exhaustiveness model and compiler experiment required teaching
  and correction. Requirement-to-test alignment remains GUIDED because the given
  input order was changed.

## Recommended next step

Before coding, complete the ordered trace for the four supplied
`ShoppingList.add(name:quantityText:)` calls: cleaned name, returned `AddResult`,
count change, exact message, and final count. Then implement validation inside
`ShoppingList`, return `AddResult` without printing in the model, and let the
caller's exhaustive switch choose the message. Later, recheck optionals and
`mutating` in a different scenario after spacing.
