# 2026-09-21 — initial assessment and Phase 1 work

Status: two sessions ended at the learner's request. Initial assessment and roadmap
are complete; the second session began Phase 1 and produced learner-reported Xcode
execution evidence for a small in-memory shopping-list behavior.

## Background and assessment conditions

Learner reports past Kotlin exposure during an Android internship, no substantial
independently built app, prior features generated by AI, and no recalled debugging
example. Initially described not having written code manually. The snippets below
were subsequently supplied in chat; no execution or provenance beyond the chat was
observed. Xcode is installed according to the learner, who reports using the GUI
Run button and simulator.

## Swift evidence

- Predicted assignment example outputs as 5 and 15 but explained the original
  value as 10; clarified that 5 was a typo and intended 10. Treated as a typo,
  not evidence of a value-semantics misconception.
- Wrote mutable customer balance, constant item price, and subtraction. Explained
  `var` versus `let` correctly. Initially omitted requested printing.
- Implemented affordability with `if balance >= price`, subtraction in the success
  branch, a failure message in `else`, and final interpolated output. A stray dot
  was identified separately from the correct logic.
- Correctly rejected subtracting a String from an Int.
- Recognized `Int("20")` as a conversion attempt, expected an integer result,
  and guessed `Int("hello")` would cause an error. Optional result semantics were
  left unresolved rather than taught during probing.
- Wrote `canAfford(balance: Int, price: Int) -> Bool` returning `balance >= price`.
  When asked to complete the omitted call, used labels, stored the return value,
  printed it, and predicted false for balance 15 and price 20.
- For prices `[12, 30, 8, 20]` and balance 20, initially looped over prices but
  called the function without argument labels and printed its Boolean result.
  After prompting, identified both issues but initially fixed only the labels.
  Following another request to trace an iteration, supplied a correct loop using
  `if canAfford(balance: balance, price: price) { print(price) }`.
  This is guided integration evidence, not independent filtering mastery.

## SwiftUI and networking evidence

- Read a provided counter view, predicted 0 initially and 2 after two button taps,
  and associated `@State` with changing UI state.
- Correctly predicted the example would restart at 0 after fully terminating and
  relaunching the app, explaining that the state did not persist across termination.
- Could not describe how a separate child view could update parent-owned count.
- Could not describe the path from opening a screen to displaying server products.
  No networking implementation knowledge was established.

## Git and debugging evidence

- Initially proposed creating a branch to save a working app version.
- With explicitly uncommitted edits, correctly said creating a branch would not
  record the working version and that a commit was needed. Also proposed another
  branch for subsequent changes; whether this was a preference or a belief that
  branches are required remains unresolved.
- For a crash, proposed reading Xcode's error and inspecting the indicated file
  and line. Breakpoint or debugger-variable inspection was not assessed.
- Diagnosed index 3 as invalid for a three-element array and proposed count minus
  one for the last index. For an empty array, predicted -1, suggested an empty
  message, then clarified that access would crash and emptiness should be checked
  first. No executable fix was requested or run.

## Planning evidence and next step

Asked to break a shopping-list app into smaller tasks (add names, display items,
mark purchased), the learner explicitly said this is a struggle and could not
propose a breakdown. This establishes a practical planning gap without establishing
knowledge or ignorance of every named architecture pattern.

Initial assessment is sufficient to choose a starting boundary: connect existing
small-code abilities to a guided requirements-decomposition and implementation
exercise. Model design and remaining Swift prerequisites still need targeted
probes when introduced. Continue toward executed practice in Xcode; avoid extending
the initial assessment into a quiz on every framework.

## Planning checkpoint

- Learner committed to 5 hours/day, 5 days/week: 25 hours/week.
- Primary target is local employment in Indonesia; international remote roles are
  welcome, but relocation abroad is not currently acceptable.
- Created a 16-week baseline with a week-12 readiness checkpoint. Selected two
  portfolio apps as the core target, preceded by a guided practice app and followed
  by an unfamiliar requirements-only hiring simulation.
- Reviewed Indonesian employer postings and official Swift/Apple/Git resources.
  Included introductory UIKit maintenance after core skills. The detailed AVOWS
  junior–middle posting is closed; it informs skills, not current job availability.
- Read-only local commands returned `Xcode 27.0`, build `27A266a`, and Apple Swift
  `6.4` (`swiftlang-6.4.0.34.1`). These establish the selected local toolchain, not
  evidence of the learner's independent tool use.
- No learner app was generated, compiled, or changed. No new mastery evidence was
  created by planning or tutor toolchain inspection.

## First teaching step and stopping point

After presenting the roadmap, introduced a small requirements-decomposition
method: choose one user action, identify the information available before it,
describe the change, and specify an observable result.

Assigned this exercise:

```text
Current shopping list: Milk, Eggs
Entered item name:    Bread
User action:          Press Add
```

Asked the learner to write 2–4 plain-language steps for the action and include the
resulting list, focusing on data before Swift syntax. The learner ended the
session without attempting it. Do not infer understanding or promote mastery
from the explanation alone. The teaching aid is at
`diagrams/2026-09-21-requirements-to-tasks.md`.

## End-of-session assessment

- Independently demonstrated narrow in-chat tasks: variable mutation/constants,
  purchase conditions, a Bool-returning function and labeled call, basic type
  distinctions, and reasoning about array bounds/empty arrays.
- Needed review prompts to combine iteration, a predicate, and conditional output.
- SwiftUI local-state behavior and Git snapshots have conceptual evidence only;
  optional conversion, cross-view state, networking, and feature planning remain
  learning gaps. Other unassessed topics must not be treated as established gaps.
- Practical work consisted of the submitted snippets and code-reading/debugging
  exercises above. No learner Xcode lab or app milestone was completed, and no
  learner code execution was observed.
- No skill was marked retained or transferable. Review targets are recorded in
  REVIEW.md; detailed learner implications are in STATE.md.

## Recommended next session

Use one brief retrieval exercise if useful, prioritizing the guided filtering
skill without repeating the entire initial assessment. Then resume the pending
Add-item decomposition exercise above and let the learner attempt it before
giving additional hints. Turn the resulting steps into a small learner-written
Swift program, predict its output, run it in Xcode, and inspect the actual result.
Probe model/optional prerequisites when they become necessary; avoid launching
the full multi-screen app before the first small behavior is understood.

## Second session — requirements, validation, and a small data model

The session resumed the pending Add-item decomposition. The learner described the
initial list, entered name, Add action, and resulting list. For later blank and
duplicate scenarios, prompts were needed to include the required message, state the
exact final list, and place validation before mutation.

### Executed Add and validation work

- Wrote and learner-reported running a mutable `[String]` program that appended
  `Bread` and printed `Milk`, `Eggs`, `Bread`; prediction matched actual output.
- Added an empty-input branch using `isEmpty`, reporting the required message and
  preserving the list. Executed both empty and valid branches successfully.
- Learned to normalize input with Foundation's
  `trimmingCharacters(in: .whitespacesAndNewlines)`. Executed surrounding-space
  input (`"   Bread  "`) and whitespace-only input; outputs matched after correcting
  missing `import Foundation` and exact prediction text.
- Added exact, case-sensitive duplicate rejection. Initially described appending
  before duplicate validation, then corrected the order to clean, validate empty,
  validate duplicate, and append only when valid.

### `ShoppingItem` model and mutation evidence

- Identified that marking an item purchased requires a name plus a Bool rather than
  a plain string, with only the Swift spelling `Bool` needing correction.
- Used the supplied struct shape to create Milk, Eggs, and Bread, changed Eggs via
  `items[1].isPurchased`, and printed `[ ] Milk`, `[x] Eggs`, `[ ] Bread`. The
  learner-reported actual output matched the prediction.
- Replaced the fixed position with a search through `items.indices` and mutated the
  matching stored element. The learner wrote and ran the complete code correctly.
- Predicted and learner-reported the value-semantics experiment: after copying an
  unpurchased `ShoppingItem` and changing the copy, the original printed `false`
  and the copy printed `true`. Correctly transferred that reasoning to an element
  copied out of an array.

### Integrated program and debugging evidence

The learner combined cleaning, duplicate detection, conditional append, the struct
model, and status display. The first attempt mixed the earlier Mark Purchased action
into Add, started with Bread already present, and predicted a different scenario.
When asked to trace the actual code, the learner correctly predicted:

```text
Item already exists
[ ] Milk
[x] Eggs
[ ] Bread
```

After targeted prompts, the learner corrected the initial data, appended a new
unpurchased `ShoppingItem` only when nonduplicate, and learner-reported successful
runs for all three paths:

```text
valid Bread       -> [ ] Milk, [ ] Eggs, [ ] Bread
duplicate Eggs    -> Item already exists; unchanged list
whitespace-only   -> Item name cannot be empty; unchanged list
```

Recurring execution-detail issue: otherwise correct reasoning often omitted a
requested output, used a different test input or initial state, omitted the
Foundation import, or mismatched exact capitalization/marker spacing. Treat this
as a requirement-tracing and verification target, not a Swift-concept failure in
every instance.

### Stopping point and next step

The next task was assigned but not attempted: implement
`containsItem(named:items:) -> Bool` using the existing `ShoppingItem` type, return
true for an exact name match and false otherwise, print nothing, and predict calls
for missing Bread and existing Eggs. Resume there, execute both cases, then use the
function to refactor duplicate detection in the integrated program. No Phase 1
milestone is complete, and no skill is yet RETAINED or TRANSFERABLE.
