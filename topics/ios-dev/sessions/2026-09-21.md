# Session: 2026-09-21

Status: ended at the learner's request. Initial probe, roadmap, and first executable Swift exercise completed.

## Starting context

Learner requested a new iOS learning path with a roughly 3–4 month junior-employment target, Indonesia first with international options. Wants hands-on projects, general programming foundations, and the ability to create personal portfolio apps. Explicitly requested progressive probing without teaching or revealed answers, followed by a dependency-based roadmap.

Initial learning files contained headings only; no earlier sessions were present.

Learner reports prior JavaScript, Python, Kotlin, and recent Swift exposure, with little manual coding and substantial AI dependence. Reports difficulty across starting, implementing, understanding, and debugging programs. Can study five hours daily; has a Mac and Xcode. Recently made a Unix-command learning app in SwiftUI, mostly with AI. Reports Git use but no independent documentation-based problem solving and no debugger use.

## Probe evidence

### 1. Assignment and reassignment

Presented:

```python
coins = 4
bonus = coins + 3
coins = 10
print(bonus)
```

Learner independently predicted 7 and explained that bonus was calculated while coins was 4. Reported no confusing syntax.

### 2. Function and conditional construction

Requirement: return shipping fee 0 for an order total at least 50, otherwise 5.

After an initial empty function shell, learner submitted:

```text
func shipping_fee(order_total) {
if order_total >= 50:
return 0
else:
return 5
}
```

Explained using order_total as an input, the condition, and why else covers the other case. Algorithm matches the requirement, including the boundary. Syntax mixes Swift and Python; no compilation was attempted. Tutor acknowledged logic and noted syntax without teaching it during the probe.

### 3. Collection and accumulator

Requirement: count scores at least 60 for arbitrary-length input. Example: [40, 70, 90, 55].

Learner predicted 2 for 70 and 90 and submitted this intended structure (chat indentation normalized here):

```text
func count_passes(scores):
    count = 0
    for score in scores:
        if score >= 60:
            count += 1
    return count
```

Independently chose initialization, traversal, selection, increment, and final return. No evidence yet for executable Swift or empty-input testing.

### 4. Debugging by inspection

Presented Python summation code with return inside the loop, called on [3, 5, 2]. Learner predicted 3 and proposed moving return outside the loop, explaining that the function otherwise stops after the first addition. Reported no unfamiliar syntax. No debugger was used.

### 5. Data modeling and output requirements

Requirement: represent Read (completed) and Walk (unfinished), then return names of unfinished habits for arbitrary inputs.

Learner independently proposed an array of custom Habit values with name (string) and completion_status (Boolean). Submitted:

```text
def unfinished_habits(habits: [Habit]):
    unfinished_habits = []
    for habit in habits:
        if habit.completion_status is False:
            unfinished_habits.append(habit)
    return unfinished_habits
```

Selection was appropriate, but output elements were whole habits rather than names. Tutor stated there was an output mismatch and asked the learner to trace the actual contents against the requirement. Learner corrected the append target to habit.name. This is guided correction, not an independent first-pass success or an established conceptual misconception.

## Assessment consequences

Evidence supports basic algorithm reasoning in small bounded tasks. The immediate boundary is consistent executable Swift and an independent development workflow. Larger decomposition, UI/state competence, and actual Git use remain unverified. There was no need to continue elementary tracing; probe ended after practical background questions.

No retention or transfer claims were made. The existing AI-assisted app was not treated as independent mastery evidence.

## Planning and verification work

- Created GOAL.md, STATE.md, REVIEW.md, ROADMAP.md, and selected RESOURCES.md entries at this meaningful initialization checkpoint.
- Planned six phases over roughly 16 weeks: executable Swift, local SwiftUI app, networked app, UIKit maintenance, independent project completion, and portfolio/interview preparation. Readiness review at week 12; applications can begin earlier when supported by evidence.
- Chose two evolving app projects, with a bounded UIKit exercise. Detailed app domains remain provisional; the networked project will be chosen around the learner's actual needs.
- Consulted official Swift/Apple/Git references and a small sample of Indonesian role postings. Search-indexed job text supports curriculum calibration; vacancy availability was not confirmed. Detailed sources are in RESOURCES.md.
- Read-only machine checks reported Xcode 27.0 (27A266a) and Apple Swift 6.4, targeting arm64-apple-macosx27.2.0. These are tutor observations, not learner-performed setup evidence.
- At this planning checkpoint, no learner exercise code had been compiled, run, or altered. No app was created or modified.

## First executable Swift exercise

After the probe, tutor explained typed Swift parameters, return types, and labeled calls using a separate doubled(number:) example. Learner rewrote shippingFee with correct parameter/return types, but initially used a colon to open the function body and reversed the shipping-fee results. Tutor identified the brace issue and prompted comparison with the original requirement. Learner corrected both.

Tutor saved the learner's corrected implementation, with normalized formatting, in practice/01-functions/main.swift. Learner added calls for 49 and 51 alongside 50. File inspection confirmed:

```swift
func shippingFee(order_total: Int) -> Int {
    if order_total >= 50 {
        return 0
    } else {
        return 5
    }
}

print(shippingFee(order_total: 50))
print(shippingFee(order_total: 49))
print(shippingFee(order_total: 51))
```

Learner predicted 0, 5, 0, then supplied terminal evidence from running:

```bash
swift /Users/raffi/Learning/topics/ios-dev/practice/01-functions/main.swift
```

Reported actual output:

```text
0
5
0
```

This establishes successful execution and correct boundary predictions with guided syntax correction and tutor-selected test inputs. It does not establish independent test design or retention. The reversed fees were corrected after revisiting the requirement; no persistent conceptual misconception is established.

## Session close

- Completed practical work: learner-written shippingFee, corrected with feedback, and three successful boundary checks in the terminal.
- Independent evidence today is limited to the bounded reasoning tasks documented above. Executable Swift functions and boundary testing remain GUIDED; no retention or transfer was assessed.
- Review priorities: reconstruct Swift function syntax, check output requirements, and choose test cases independently. Syntax mixing and reversed fees were corrected; neither establishes a persistent misconception.
- The checkoutTotal exercise was assigned but not attempted before the learner ended the session. At close, practice/01-functions/main.swift still contains only shippingFee and the three recorded calls.

## Resume next session

Use the pending checkoutTotal exercise as a short retrieval and composition task: take an Int order total, call shippingFee, and return the order total including shipping as an Int. Keep printing outside the functions. Have the learner choose one paid-shipping and one free-shipping input, predict results, run the code, and share evidence. No implementation or expected outputs have been supplied.

Let the first attempt reveal what needs refreshing; avoid re-teaching the full previous lesson. Continue toward documentation and debugger practice after this task. Phase 1 remains in progress; no curriculum phase has been completed.
